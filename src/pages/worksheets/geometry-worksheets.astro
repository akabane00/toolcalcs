---
import BaseLayout from '../../layouts/BaseLayout.astro';
import AdUnit from '../../components/AdUnit.astro';
import { getWorksheetBySlug } from '../../data/worksheets';
import '../../styles/worksheet.css';

const ws = getWorksheetBySlug('geometry-worksheets')!;

const structuredData = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://toolcalcs.com/" },
    { "@type": "ListItem", "position": 2, "name": "Worksheets", "item": "https://toolcalcs.com/worksheets/" },
    { "@type": "ListItem", "position": 3, "name": "Geometry Worksheets" }
  ]
};
---

<BaseLayout title="Free Geometry Worksheets - Printable Math Practice | ToolCalcs" description={ws.description} type="article" structuredData={structuredData}>
  <nav class="breadcrumb">
    <a href="/">Home</a> &rsaquo;
    <a href="/worksheets/">Worksheets</a> &rsaquo;
    <span>Geometry Worksheets</span>
  </nav>

  <h1>{ws.title}</h1>
  <p class="page-desc">{ws.description}</p>

  <div class="worksheet-box">
    <div class="config-panel">
      <h3>Settings</h3>
      <div class="config-inputs">
        <div class="config-group">
          <label for="cfgProblems">Number of Problems</label>
          <input type="number" id="cfgProblems" value="10" min="5" max="20" />
        </div>
        <div class="config-group">
          <label for="cfgType">Problem Type</label>
          <select id="cfgType">
            <option value="identifyShapes" selected>Identify Shapes</option>
            <option value="angles">Classify Angles</option>
            <option value="symmetry">Lines of Symmetry</option>
            <option value="classify">Classify Triangles</option>
          </select>
        </div>
        <div class="config-group">
          <label for="cfgDifficulty">Difficulty</label>
          <select id="cfgDifficulty">
            <option value="easy" selected>Easy</option>
            <option value="medium">Medium</option>
            <option value="hard">Hard</option>
          </select>
        </div>
      </div>
    </div>

    <div class="worksheet-actions">
      <button class="ws-btn primary" id="btnGenerate">Generate New</button>
      <button class="ws-btn print" id="btnPrint">Print</button>
      <label class="toggle-label">
        <input type="checkbox" id="toggleAnswers" />
        Show Answers
      </label>
    </div>

    <div class="worksheet-content" id="worksheetContent">
      <div class="worksheet-header-print">
        <h2>Geometry Practice</h2>
        <div class="name-date">
          <span>Name: ___________________</span>
          <span>Date: ___________________</span>
        </div>
      </div>
      <div class="problem-grid geo-grid" id="problemGrid"></div>
      <div class="answer-key hidden" id="answerKey">
        <h3>Answer Key</h3>
        <div class="answer-grid" id="answerGrid"></div>
      </div>
    </div>
  </div>

  <AdUnit slot="ws-mid" format="rectangle" />

  <article class="content-section">
    <h2>What Is Geometry?</h2>
    <p>Geometry is the branch of mathematics that deals with shapes, sizes, positions, and properties of space. From recognizing a circle in kindergarten to calculating the volume of a cylinder in middle school, geometry is a core part of math education. It develops spatial reasoning, logical thinking, and the ability to visualize and describe the physical world mathematically.</p>
    <p>These free printable geometry worksheets cover four key topics: shape identification, angle classification, lines of symmetry, and triangle classification. Every worksheet is randomly generated with SVG diagrams for unlimited visual practice.</p>

    <h2>Types of Geometry Problems</h2>
    <h3>Identify Shapes</h3>
    <p>Shape identification is the foundation of geometry. Students learn to recognize and name 2D shapes by their properties: number of sides, number of corners (vertices), and side lengths. Basic shapes include triangles (3 sides), squares and rectangles (4 sides), pentagons (5 sides), hexagons (6 sides), and circles. More advanced shapes include parallelograms, trapezoids, and rhombuses.</p>

    <h3>Classify Angles</h3>
    <p>Angles are formed where two lines meet. They are classified by their measure: acute angles are less than 90&deg;, right angles are exactly 90&deg;, obtuse angles are between 90&deg; and 180&deg;, and straight angles are exactly 180&deg;. Students learn to estimate angle measurements and classify angles by sight, which is essential for later work with protractors and geometric proofs.</p>

    <h3>Lines of Symmetry</h3>
    <p>A line of symmetry divides a shape into two identical halves that are mirror images of each other. A square has 4 lines of symmetry, a rectangle has 2, an equilateral triangle has 3, and a circle has infinitely many. Understanding symmetry helps students recognize patterns, create balanced designs, and prepare for concepts like reflections and transformations.</p>

    <h3>Classify Triangles</h3>
    <p>Triangles can be classified by their sides and angles. By sides: equilateral (all sides equal), isosceles (two sides equal), and scalene (no sides equal). By angles: acute (all angles less than 90&deg;), right (one angle equals 90&deg;), and obtuse (one angle greater than 90&deg;). Students learn to identify triangle types by examining their properties visually and numerically.</p>

    <h2>Grade-Level Guide</h2>
    <h3>Second Grade (Ages 7-8)</h3>
    <p>Second graders learn to identify and describe basic 2D shapes: triangles, rectangles, squares, pentagons, hexagons, and circles. They compare shapes by the number of sides and angles. Use "Identify Shapes" with "Easy" difficulty for grade-appropriate practice.</p>

    <h3>Third Grade (Ages 8-9)</h3>
    <p>Third graders explore shape properties more deeply, including parallel and perpendicular lines. They are introduced to angles (right angles first) and begin working with symmetry. Use "Angles" or "Symmetry" type with "Easy" difficulty.</p>

    <h3>Fourth Grade (Ages 9-10)</h3>
    <p>Fourth graders classify angles as acute, right, or obtuse and use protractors to measure angles. They classify triangles by sides and angles and identify lines of symmetry in complex shapes. Use "Medium" difficulty for all types.</p>

    <h3>Fifth Grade (Ages 10-11)</h3>
    <p>Fifth graders work with more complex shapes including parallelograms, trapezoids, and rhombuses. They solve problems involving angle relationships and coordinate geometry. Use "Hard" difficulty for advanced shape recognition and classification.</p>

    <h2>Tips for Teaching Geometry</h2>
    <ul>
      <li><strong>Use real-world examples.</strong> Point out shapes in the classroom, architecture, nature, and everyday objects. A clock face is a circle, a door is a rectangle, a yield sign is a triangle.</li>
      <li><strong>Build shapes with manipulatives.</strong> Use geoboards, pattern blocks, tangrams, and straws to construct shapes. Hands-on building deepens understanding of shape properties.</li>
      <li><strong>Fold paper for symmetry.</strong> Cut out shapes and fold them to find lines of symmetry. This concrete activity makes the abstract concept of symmetry tangible.</li>
      <li><strong>Sort and classify.</strong> Give students a collection of shape cards and have them sort by different attributes: number of sides, number of right angles, parallel sides, etc.</li>
      <li><strong>Practice estimation before measuring.</strong> Before using a protractor, have students estimate whether an angle is acute, right, or obtuse. This builds angle sense.</li>
    </ul>

    <h2>How to Use These Worksheets</h2>
    <p>Select the number of problems, type, and difficulty using the settings above. Click "Generate New" for a fresh worksheet with randomly generated shapes and diagrams. Use "Print" for a clean page and "Show Answers" to check work or reveal the answer key.</p>

    <h2>Related Worksheets</h2>
    <ul>
      <li><a href="/worksheets/area-and-perimeter-worksheets/">Area &amp; Perimeter Worksheets</a> - Calculate area and perimeter of shapes.</li>
      <li><a href="/worksheets/measurement-worksheets/">Measurement Worksheets</a> - Practice unit conversions and measurement.</li>
      <li><a href="/worksheets/fractions-worksheets/">Fractions Worksheets</a> - Identify and compute with fractions.</li>
      <li><a href="/worksheets/comparing-numbers-worksheets/">Comparing Numbers Worksheets</a> - Practice using comparison symbols.</li>
    </ul>
  </article>

  <AdUnit slot="ws-bottom" format="horizontal" />
</BaseLayout>

<script is:inline>
  var problems = [];

  function randInt(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }

  function regularPolygon(cx, cy, r, sides) {
    var pts = [];
    for (var i = 0; i < sides; i++) {
      var angle = (i / sides) * 2 * Math.PI - Math.PI / 2;
      pts.push((cx + r * Math.cos(angle)).toFixed(1) + ',' + (cy + r * Math.sin(angle)).toFixed(1));
    }
    return pts.join(' ');
  }

  var SHAPES_EASY = [
    { name: 'Triangle', sides: 3 },
    { name: 'Square', sides: 4, square: true },
    { name: 'Rectangle', sides: 4, rect: true },
    { name: 'Pentagon', sides: 5 },
    { name: 'Hexagon', sides: 6 },
    { name: 'Circle', sides: 0 }
  ];
  var SHAPES_MEDIUM = SHAPES_EASY.concat([
    { name: 'Octagon', sides: 8 },
    { name: 'Parallelogram', sides: 4, para: true }
  ]);
  var SHAPES_HARD = SHAPES_MEDIUM.concat([
    { name: 'Trapezoid', sides: 4, trap: true },
    { name: 'Rhombus', sides: 4, rhombus: true }
  ]);

  var SYMMETRY_DATA = {
    'Circle': 'infinite',
    'Square': 4,
    'Rectangle': 2,
    'Equilateral Triangle': 3,
    'Isosceles Triangle': 1,
    'Scalene Triangle': 0,
    'Pentagon': 5,
    'Hexagon': 6,
    'Octagon': 8,
    'Parallelogram': 0,
    'Trapezoid': 0,
    'Rhombus': 2
  };

  function drawShape(shape) {
    var size = 120;
    var cx = size / 2, cy = size / 2, r = 42;
    var svg = '<svg viewBox="0 0 ' + size + ' ' + size + '" class="geo-svg">';

    if (shape.sides === 0) {
      svg += '<circle cx="' + cx + '" cy="' + cy + '" r="' + r + '" fill="rgba(37,99,235,0.08)" stroke="#334155" stroke-width="2.5"/>';
    } else if (shape.rect) {
      var w = 80, h = 50;
      svg += '<rect x="' + (cx - w / 2) + '" y="' + (cy - h / 2) + '" width="' + w + '" height="' + h + '" fill="rgba(37,99,235,0.08)" stroke="#334155" stroke-width="2.5"/>';
    } else if (shape.square) {
      var s = 65;
      svg += '<rect x="' + (cx - s / 2) + '" y="' + (cy - s / 2) + '" width="' + s + '" height="' + s + '" fill="rgba(37,99,235,0.08)" stroke="#334155" stroke-width="2.5"/>';
    } else if (shape.para) {
      var w = 75, h = 50, skew = 18;
      svg += '<polygon points="' + (cx - w / 2 + skew) + ',' + (cy - h / 2) + ' ' + (cx + w / 2 + skew) + ',' + (cy - h / 2) + ' ' + (cx + w / 2 - skew) + ',' + (cy + h / 2) + ' ' + (cx - w / 2 - skew) + ',' + (cy + h / 2) + '" fill="rgba(37,99,235,0.08)" stroke="#334155" stroke-width="2.5"/>';
    } else if (shape.trap) {
      var topW = 40, botW = 80, h = 50;
      svg += '<polygon points="' + (cx - topW / 2) + ',' + (cy - h / 2) + ' ' + (cx + topW / 2) + ',' + (cy - h / 2) + ' ' + (cx + botW / 2) + ',' + (cy + h / 2) + ' ' + (cx - botW / 2) + ',' + (cy + h / 2) + '" fill="rgba(37,99,235,0.08)" stroke="#334155" stroke-width="2.5"/>';
    } else if (shape.rhombus) {
      svg += '<polygon points="' + cx + ',' + (cy - r) + ' ' + (cx + r * 0.7) + ',' + cy + ' ' + cx + ',' + (cy + r) + ' ' + (cx - r * 0.7) + ',' + cy + '" fill="rgba(37,99,235,0.08)" stroke="#334155" stroke-width="2.5"/>';
    } else {
      svg += '<polygon points="' + regularPolygon(cx, cy, r, shape.sides) + '" fill="rgba(37,99,235,0.08)" stroke="#334155" stroke-width="2.5"/>';
    }

    svg += '</svg>';
    return svg;
  }

  function drawAngle(degrees) {
    var size = 120;
    var cx = 20, cy = 95;
    var len = 80;
    var rad = degrees * Math.PI / 180;
    var x2 = cx + len * Math.cos(-rad);
    var y2 = cy + len * Math.sin(-rad);

    var svg = '<svg viewBox="0 0 ' + size + ' ' + size + '" class="geo-svg">';
    svg += '<line x1="' + cx + '" y1="' + cy + '" x2="' + (cx + len) + '" y2="' + cy + '" stroke="#334155" stroke-width="2.5"/>';
    svg += '<line x1="' + cx + '" y1="' + cy + '" x2="' + x2.toFixed(1) + '" y2="' + y2.toFixed(1) + '" stroke="#334155" stroke-width="2.5"/>';

    // Arc
    var arcR = 20;
    var ax = cx + arcR;
    var ay = cy;
    var bx = cx + arcR * Math.cos(-rad);
    var by = cy + arcR * Math.sin(-rad);
    var large = degrees > 180 ? 1 : 0;
    svg += '<path d="M ' + ax + ' ' + ay + ' A ' + arcR + ' ' + arcR + ' 0 ' + large + ' 0 ' + bx.toFixed(1) + ' ' + by.toFixed(1) + '" fill="none" stroke="#2563eb" stroke-width="1.5"/>';

    svg += '<text x="' + (cx + 30) + '" y="' + (cy - 5) + '" font-size="12" fill="#2563eb" font-weight="600">' + degrees + '\u00b0</text>';
    svg += '</svg>';
    return svg;
  }

  function drawTriangleType(type) {
    var size = 120;
    var svg = '<svg viewBox="0 0 ' + size + ' ' + size + '" class="geo-svg">';
    var pts;

    if (type === 'equilateral') {
      pts = regularPolygon(60, 65, 42, 3);
    } else if (type === 'isosceles') {
      pts = '60,15 25,95 95,95';
    } else if (type === 'scalene') {
      pts = '15,90 50,15 100,90';
    } else if (type === 'right') {
      pts = '20,90 20,20 100,90';
      // Right angle marker
      svg += '<polyline points="20,35 35,35 35,90" fill="none" stroke="#2563eb" stroke-width="1.5"/>';
    }

    svg += '<polygon points="' + pts + '" fill="rgba(37,99,235,0.08)" stroke="#334155" stroke-width="2.5" stroke-linejoin="round"/>';
    svg += '</svg>';
    return svg;
  }

  function drawSymmetryShape(shapeName) {
    if (shapeName === 'Circle') return drawShape({ name: 'Circle', sides: 0 });
    if (shapeName === 'Square') return drawShape({ name: 'Square', sides: 4, square: true });
    if (shapeName === 'Rectangle') return drawShape({ name: 'Rectangle', sides: 4, rect: true });
    if (shapeName === 'Equilateral Triangle') return drawShape({ name: 'Triangle', sides: 3 });
    if (shapeName === 'Isosceles Triangle') {
      var svg = '<svg viewBox="0 0 120 120" class="geo-svg">';
      svg += '<polygon points="60,15 25,95 95,95" fill="rgba(37,99,235,0.08)" stroke="#334155" stroke-width="2.5"/>';
      svg += '</svg>';
      return svg;
    }
    if (shapeName === 'Pentagon') return drawShape({ name: 'Pentagon', sides: 5 });
    if (shapeName === 'Hexagon') return drawShape({ name: 'Hexagon', sides: 6 });
    if (shapeName === 'Octagon') return drawShape({ name: 'Octagon', sides: 8 });
    if (shapeName === 'Parallelogram') return drawShape({ name: 'Parallelogram', sides: 4, para: true });
    if (shapeName === 'Rhombus') return drawShape({ name: 'Rhombus', sides: 4, rhombus: true });
    if (shapeName === 'Trapezoid') return drawShape({ name: 'Trapezoid', sides: 4, trap: true });
    return drawShape({ name: 'Square', sides: 4, square: true });
  }

  function generateProblems() {
    var count = parseInt(document.getElementById('cfgProblems').value, 10);
    var type = document.getElementById('cfgType').value;
    var diff = document.getElementById('cfgDifficulty').value;

    if (isNaN(count) || count < 5) count = 5;
    if (count > 20) count = 20;

    problems = [];
    for (var i = 0; i < count; i++) {
      var p = {};

      if (type === 'identifyShapes') {
        var pool = diff === 'easy' ? SHAPES_EASY : diff === 'medium' ? SHAPES_MEDIUM : SHAPES_HARD;
        var shape = pool[randInt(0, pool.length - 1)];
        p.svg = drawShape(shape);
        p.question = 'Name this shape:';
        p.answer = shape.name;
        p.answerStr = shape.name;
      } else if (type === 'angles') {
        var degrees;
        if (diff === 'easy') {
          degrees = [30, 45, 60, 90, 120, 135, 150][randInt(0, 6)];
        } else if (diff === 'medium') {
          degrees = randInt(1, 11) * 15;
        } else {
          degrees = randInt(10, 170);
        }
        p.svg = drawAngle(degrees);
        p.question = 'Classify this angle:';
        if (degrees < 90) p.answer = 'Acute';
        else if (degrees === 90) p.answer = 'Right';
        else p.answer = 'Obtuse';
        p.answerStr = p.answer + ' (' + degrees + '\u00b0)';
      } else if (type === 'symmetry') {
        var shapes;
        if (diff === 'easy') {
          shapes = ['Circle', 'Square', 'Rectangle', 'Equilateral Triangle'];
        } else if (diff === 'medium') {
          shapes = ['Circle', 'Square', 'Rectangle', 'Equilateral Triangle', 'Isosceles Triangle', 'Pentagon', 'Hexagon'];
        } else {
          shapes = Object.keys(SYMMETRY_DATA);
        }
        var shapeName = shapes[randInt(0, shapes.length - 1)];
        p.svg = drawSymmetryShape(shapeName);
        p.question = 'How many lines of symmetry?';
        var sym = SYMMETRY_DATA[shapeName];
        p.answer = sym === 'infinite' ? 'Infinite' : String(sym);
        p.answerStr = p.answer + (sym !== 'infinite' && sym !== 0 ? ' line' + (sym > 1 ? 's' : '') : '');
      } else if (type === 'classify') {
        var types;
        if (diff === 'easy') {
          types = ['equilateral', 'isosceles', 'right'];
        } else {
          types = ['equilateral', 'isosceles', 'scalene', 'right'];
        }
        var triType = types[randInt(0, types.length - 1)];
        p.svg = drawTriangleType(triType);
        p.question = 'Classify this triangle:';
        p.answer = triType.charAt(0).toUpperCase() + triType.slice(1);
        p.answerStr = p.answer;
      }

      problems.push(p);
    }

    renderProblems();
    renderAnswerKey();
    updateAnswerVisibility();
  }

  function renderProblems() {
    var grid = document.getElementById('problemGrid');
    var showAnswers = document.getElementById('toggleAnswers').checked;
    var html = '';

    for (var i = 0; i < problems.length; i++) {
      var p = problems[i];
      html += '<div class="problem geo-problem">';
      html += '<div class="problem-number">' + (i + 1) + ')</div>';
      html += p.svg;
      html += '<div class="geo-question">' + p.question + '</div>';
      html += '<div class="problem-answer' + (showAnswers ? ' revealed' : '') + '">';
      html += showAnswers ? p.answerStr : '_______________';
      html += '</div>';
      html += '</div>';
    }

    grid.innerHTML = html;
  }

  function renderAnswerKey() {
    var answerGrid = document.getElementById('answerGrid');
    var html = '';
    for (var i = 0; i < problems.length; i++) {
      html += '<div class="answer-item">';
      html += '<span class="q">' + (i + 1) + '.</span>';
      html += '<span class="a">' + problems[i].answerStr + '</span>';
      html += '</div>';
    }
    answerGrid.innerHTML = html;
  }

  function updateAnswerVisibility() {
    var showAnswers = document.getElementById('toggleAnswers').checked;
    var answerKey = document.getElementById('answerKey');
    if (showAnswers) { answerKey.classList.remove('hidden'); }
    else { answerKey.classList.add('hidden'); }
    renderProblems();
  }

  document.getElementById('btnGenerate').addEventListener('click', generateProblems);
  document.getElementById('btnPrint').addEventListener('click', function () { window.print(); });
  document.getElementById('toggleAnswers').addEventListener('change', updateAnswerVisibility);
  generateProblems();
</script>

<style>
  :global(.geo-grid) {
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)) !important;
  }
  :global(.geo-problem) {
    flex-direction: column !important;
    align-items: center !important;
    min-height: 180px;
    padding: 14px !important;
  }
  :global(.geo-svg) {
    width: 100%;
    max-width: 120px;
    height: auto;
    display: block;
    margin: 0 auto 8px;
  }
  :global(.geo-question) {
    font-size: 0.9rem;
    text-align: center;
    margin-bottom: 6px;
    color: var(--color-text-muted);
  }
  :global(.geo-problem .problem-answer) {
    font-size: 0.9rem;
  }
  @media (max-width: 640px) {
    :global(.geo-grid) { grid-template-columns: repeat(2, 1fr) !important; }
  }
  @media print {
    :global(.geo-svg polygon), :global(.geo-svg rect), :global(.geo-svg circle) {
      fill: rgba(0,0,0,0.05) !important;
      stroke: #333 !important;
    }
    :global(.geo-svg text) { fill: #333 !important; }
    :global(.geo-svg path) { stroke: #333 !important; }
    :global(.geo-svg line) { stroke: #333 !important; }
  }
</style>
