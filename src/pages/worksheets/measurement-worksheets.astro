---
import BaseLayout from '../../layouts/BaseLayout.astro';
import AdUnit from '../../components/AdUnit.astro';
import { getWorksheetBySlug } from '../../data/worksheets';
import '../../styles/worksheet.css';

const ws = getWorksheetBySlug('measurement-worksheets')!;

const structuredData = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://toolcalcs.com/" },
    { "@type": "ListItem", "position": 2, "name": "Worksheets", "item": "https://toolcalcs.com/worksheets/" },
    { "@type": "ListItem", "position": 3, "name": "Measurement Worksheets" }
  ]
};
---

<BaseLayout title="Free Measurement Worksheets - Printable Math Practice | ToolCalcs" description={ws.description} type="article" structuredData={structuredData}>
  <nav class="breadcrumb">
    <a href="/">Home</a> &rsaquo;
    <a href="/worksheets/">Worksheets</a> &rsaquo;
    <span>Measurement Worksheets</span>
  </nav>

  <h1>{ws.title}</h1>
  <p class="page-desc">{ws.description}</p>

  <div class="worksheet-box">
    <div class="config-panel">
      <h3>Settings</h3>
      <div class="config-inputs">
        <div class="config-group">
          <label for="cfgProblems">Number of Problems</label>
          <input type="number" id="cfgProblems" value="15" min="5" max="30" />
        </div>
        <div class="config-group">
          <label for="cfgType">Problem Type</label>
          <select id="cfgType">
            <option value="convert" selected>Unit Conversions</option>
            <option value="compare">Compare Measurements</option>
            <option value="wordProblem">Word Problems</option>
          </select>
        </div>
        <div class="config-group">
          <label for="cfgSystem">Measurement System</label>
          <select id="cfgSystem">
            <option value="customary" selected>Customary (US)</option>
            <option value="metric">Metric</option>
            <option value="both">Both</option>
          </select>
        </div>
      </div>
    </div>

    <div class="worksheet-actions">
      <button class="ws-btn primary" id="btnGenerate">Generate New</button>
      <button class="ws-btn print" id="btnPrint">Print</button>
      <label class="toggle-label">
        <input type="checkbox" id="toggleAnswers" />
        Show Answers
      </label>
    </div>

    <div class="worksheet-content" id="worksheetContent">
      <div class="worksheet-header-print">
        <h2>Measurement Practice</h2>
        <div class="name-date">
          <span>Name: ___________________</span>
          <span>Date: ___________________</span>
        </div>
      </div>
      <div class="problem-grid msr-grid" id="problemGrid"></div>
      <div class="answer-key hidden" id="answerKey">
        <h3>Answer Key</h3>
        <div class="answer-grid" id="answerGrid"></div>
      </div>
    </div>
  </div>

  <AdUnit slot="ws-mid" format="rectangle" />

  <article class="content-section">
    <h2>Why Measurement Matters</h2>
    <p>Measurement is one of the most practical math skills students learn. From cooking and crafting to building and traveling, the ability to measure and convert between units is used daily. Students need to understand both the US customary system (inches, feet, cups, pounds) and the metric system (centimeters, meters, liters, grams) to succeed in math, science, and real-world applications.</p>
    <p>These free printable measurement worksheets give students practice converting between units, comparing measurements, and solving word problems involving measurement. Every worksheet is randomly generated for unlimited practice.</p>

    <h2>US Customary Units</h2>
    <h3>Length</h3>
    <p>The customary length units are inches, feet, yards, and miles. There are 12 inches in a foot, 3 feet in a yard, and 5,280 feet in a mile. Students typically learn inch-foot and foot-yard conversions in second and third grade, with miles introduced later.</p>

    <h3>Capacity</h3>
    <p>Capacity measures how much liquid a container can hold. The customary units are cups, pints, quarts, and gallons. The relationships follow a pattern of doubling: 2 cups = 1 pint, 2 pints = 1 quart, 4 quarts = 1 gallon. The mnemonic "2 cups in a pint, 2 pints in a quart, 4 quarts in a gallon" helps students remember.</p>

    <h3>Weight</h3>
    <p>The customary weight units are ounces and pounds, with 16 ounces in one pound. Students practice weighing objects and converting between these units, building practical skills for cooking and shopping.</p>

    <h2>Metric Units</h2>
    <p>The metric system uses a base-10 structure that makes conversions straightforward. For length: 100 centimeters = 1 meter, 1,000 meters = 1 kilometer. For capacity: 1,000 milliliters = 1 liter. For mass: 1,000 grams = 1 kilogram. The consistent factor-of-10 pattern makes the metric system easier to learn once students understand the prefixes (milli-, centi-, kilo-).</p>

    <h2>Grade-Level Guide</h2>
    <h3>Second Grade (Ages 7-8)</h3>
    <p>Second graders learn to measure length using standard units and compare lengths of objects. They begin with inches and centimeters, using rulers to measure physical objects. Start with simple conversions between inches and feet using the "Customary" setting.</p>

    <h3>Third Grade (Ages 8-9)</h3>
    <p>Third graders extend measurement to capacity and weight. They learn cups, pints, quarts, gallons, ounces, and pounds. They also begin metric measurement with centimeters, meters, grams, and kilograms. Use the "Both" system setting for comprehensive practice.</p>

    <h3>Fourth Grade (Ages 9-10)</h3>
    <p>Fourth graders convert within a single measurement system, working with larger numbers and multi-step conversions. They solve word problems involving measurement and begin using measurement in other areas like area and perimeter. Use "Word Problems" type for applied practice.</p>

    <h2>Tips for Teaching Measurement</h2>
    <ul>
      <li><strong>Use real objects.</strong> Have students measure actual items in the classroom with rulers, measuring cups, and scales. Physical experience makes units meaningful.</li>
      <li><strong>Create reference points.</strong> A paper clip is about 1 inch, a door is about 1 meter, a large water bottle is about 1 liter. These benchmarks help students estimate measurements.</li>
      <li><strong>Practice conversions systematically.</strong> Teach students to multiply when converting from larger to smaller units and divide when converting from smaller to larger units.</li>
      <li><strong>Use conversion charts.</strong> Post a measurement chart in the classroom and encourage students to reference it until the relationships become automatic.</li>
      <li><strong>Connect to cooking.</strong> Recipes are excellent real-world measurement applications. Doubling or halving a recipe requires conversion skills.</li>
    </ul>

    <h2>How to Use These Worksheets</h2>
    <p>Select the number of problems, problem type, and measurement system using the settings above. Click "Generate New" for a fresh worksheet. Use "Print" for a clean page and "Show Answers" to check work or reveal the answer key.</p>

    <h2>Related Worksheets</h2>
    <ul>
      <li><a href="/worksheets/area-and-perimeter-worksheets/">Area &amp; Perimeter Worksheets</a> - Calculate area and perimeter of shapes.</li>
      <li><a href="/worksheets/decimals-worksheets/">Decimals Worksheets</a> - Practice decimal arithmetic for measurement calculations.</li>
      <li><a href="/worksheets/multiplication-worksheets/">Multiplication Worksheets</a> - Build multiplication fluency for unit conversions.</li>
      <li><a href="/worksheets/word-problems-worksheets/">Word Problems Worksheets</a> - Solve math word problems across all operations.</li>
    </ul>
  </article>

  <AdUnit slot="ws-bottom" format="horizontal" />
</BaseLayout>

<script is:inline>
  var problems = [];

  function randInt(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }

  var CUSTOMARY = [
    { from: 'inches', to: 'feet', factor: 12, fromAbbr: 'in', toAbbr: 'ft', category: 'length' },
    { from: 'feet', to: 'yards', factor: 3, fromAbbr: 'ft', toAbbr: 'yd', category: 'length' },
    { from: 'cups', to: 'pints', factor: 2, fromAbbr: 'c', toAbbr: 'pt', category: 'capacity' },
    { from: 'pints', to: 'quarts', factor: 2, fromAbbr: 'pt', toAbbr: 'qt', category: 'capacity' },
    { from: 'quarts', to: 'gallons', factor: 4, fromAbbr: 'qt', toAbbr: 'gal', category: 'capacity' },
    { from: 'ounces', to: 'pounds', factor: 16, fromAbbr: 'oz', toAbbr: 'lb', category: 'weight' }
  ];

  var METRIC = [
    { from: 'centimeters', to: 'meters', factor: 100, fromAbbr: 'cm', toAbbr: 'm', category: 'length' },
    { from: 'meters', to: 'kilometers', factor: 1000, fromAbbr: 'm', toAbbr: 'km', category: 'length' },
    { from: 'milliliters', to: 'liters', factor: 1000, fromAbbr: 'mL', toAbbr: 'L', category: 'capacity' },
    { from: 'grams', to: 'kilograms', factor: 1000, fromAbbr: 'g', toAbbr: 'kg', category: 'mass' }
  ];

  var NAMES = ['Emma', 'Liam', 'Olivia', 'Noah', 'Ava', 'James', 'Sophia', 'Mason'];

  function getConversions(system) {
    if (system === 'customary') return CUSTOMARY;
    if (system === 'metric') return METRIC;
    return CUSTOMARY.concat(METRIC);
  }

  function generateProblems() {
    var count = parseInt(document.getElementById('cfgProblems').value, 10);
    var type = document.getElementById('cfgType').value;
    var system = document.getElementById('cfgSystem').value;
    var convs = getConversions(system);

    if (isNaN(count) || count < 5) count = 5;
    if (count > 30) count = 30;

    problems = [];
    for (var i = 0; i < count; i++) {
      var conv = convs[randInt(0, convs.length - 1)];
      var p = {};

      if (type === 'convert') {
        var toSmall = Math.random() < 0.5;
        if (toSmall) {
          var val = randInt(1, 10);
          p.text = val + ' ' + conv.to + ' = ___ ' + conv.from;
          p.answer = val * conv.factor;
          p.answerStr = p.answer + ' ' + conv.fromAbbr;
        } else {
          var val = randInt(1, 10) * conv.factor;
          p.text = val + ' ' + conv.from + ' = ___ ' + conv.to;
          p.answer = val / conv.factor;
          p.answerStr = p.answer + ' ' + conv.toAbbr;
        }
      } else if (type === 'compare') {
        var valA, unitA, valB, unitB, inBase;
        if (Math.random() < 0.5) {
          valA = randInt(1, 8);
          unitA = conv.to;
          valB = randInt(1, valA * conv.factor + conv.factor);
          unitB = conv.from;
          var aBase = valA * conv.factor;
          p.answer = aBase > valB ? '>' : aBase < valB ? '<' : '=';
        } else {
          valA = randInt(1, 10) * conv.factor;
          unitA = conv.from;
          valB = randInt(1, Math.ceil(valA / conv.factor) + 2);
          unitB = conv.to;
          var bBase = valB * conv.factor;
          p.answer = valA > bBase ? '>' : valA < bBase ? '<' : '=';
        }
        p.text = valA + ' ' + unitA + '  ___  ' + valB + ' ' + unitB;
        p.answerStr = p.answer;
        p.isCompare = true;
      } else {
        var name = NAMES[randInt(0, NAMES.length - 1)];
        var templates = [];
        if (conv.category === 'length') {
          var val = randInt(2, 8);
          templates.push({
            text: name + ' has a ribbon that is ' + (val * conv.factor) + ' ' + conv.from + ' long. How many ' + conv.to + ' is that?',
            answer: val,
            unit: conv.toAbbr
          });
          templates.push({
            text: name + ' measured a board to be ' + val + ' ' + conv.to + ' long. How many ' + conv.from + ' is that?',
            answer: val * conv.factor,
            unit: conv.fromAbbr
          });
        } else if (conv.category === 'capacity') {
          var val = randInt(2, 6);
          templates.push({
            text: name + ' needs ' + (val * conv.factor) + ' ' + conv.from + ' of water. How many ' + conv.to + ' is that?',
            answer: val,
            unit: conv.toAbbr
          });
        } else {
          var val = randInt(2, 5);
          templates.push({
            text: name + ' has ' + (val * conv.factor) + ' ' + conv.from + ' of flour. How many ' + conv.to + ' is that?',
            answer: val,
            unit: conv.toAbbr
          });
        }
        var t = templates[randInt(0, templates.length - 1)];
        p.text = t.text;
        p.answer = t.answer;
        p.answerStr = t.answer + ' ' + t.unit;
      }

      problems.push(p);
    }

    renderProblems();
    renderAnswerKey();
    updateAnswerVisibility();
  }

  function renderProblems() {
    var grid = document.getElementById('problemGrid');
    var showAnswers = document.getElementById('toggleAnswers').checked;
    var type = document.getElementById('cfgType').value;
    var html = '';

    for (var i = 0; i < problems.length; i++) {
      var p = problems[i];
      html += '<div class="problem msr-problem">';
      html += '<div class="problem-number">' + (i + 1) + ')</div>';

      if (p.isCompare) {
        var parts = p.text.split('___');
        html += '<div class="cmp-row">';
        html += '<span class="cmp-num">' + parts[0].trim() + '</span>';
        html += '<span class="cmp-box' + (showAnswers ? ' revealed' : '') + '">';
        html += showAnswers ? p.answer : '';
        html += '</span>';
        html += '<span class="cmp-num">' + parts[1].trim() + '</span>';
        html += '</div>';
      } else {
        html += '<div class="msr-text">' + p.text + '</div>';
        html += '<div class="problem-answer' + (showAnswers ? ' revealed' : '') + '">';
        html += showAnswers ? p.answerStr : (type === 'wordProblem' ? '_______________' : '___');
        html += '</div>';
      }

      html += '</div>';
    }

    grid.innerHTML = html;
  }

  function renderAnswerKey() {
    var answerGrid = document.getElementById('answerGrid');
    var html = '';
    for (var i = 0; i < problems.length; i++) {
      html += '<div class="answer-item">';
      html += '<span class="q">' + (i + 1) + '.</span>';
      html += '<span class="a">' + problems[i].answerStr + '</span>';
      html += '</div>';
    }
    answerGrid.innerHTML = html;
  }

  function updateAnswerVisibility() {
    var showAnswers = document.getElementById('toggleAnswers').checked;
    var answerKey = document.getElementById('answerKey');
    if (showAnswers) { answerKey.classList.remove('hidden'); }
    else { answerKey.classList.add('hidden'); }
    renderProblems();
  }

  document.getElementById('btnGenerate').addEventListener('click', generateProblems);
  document.getElementById('btnPrint').addEventListener('click', function () { window.print(); });
  document.getElementById('toggleAnswers').addEventListener('change', updateAnswerVisibility);
  generateProblems();
</script>

<style>
  :global(.msr-grid) {
    grid-template-columns: 1fr !important;
    gap: 8px !important;
  }
  :global(.msr-problem) {
    text-align: left !important;
    padding: 12px 16px !important;
    display: flex !important;
    flex-direction: row !important;
    align-items: center !important;
    gap: 12px;
    border-bottom: 1px solid var(--color-border);
  }
  :global(.msr-text) {
    font-size: 1rem;
    line-height: 1.6;
    flex: 1;
  }
  :global(.msr-problem .problem-answer) {
    min-width: 80px;
    text-align: center;
  }
  :global(.cmp-row) { display: flex; align-items: center; gap: 12px; font-size: 1.1rem; }
  :global(.cmp-num) { font-weight: 600; min-width: 48px; text-align: center; }
  :global(.cmp-box) { display: inline-flex; align-items: center; justify-content: center; width: 40px; height: 36px; border: 2px solid var(--color-border); border-radius: 4px; font-size: 1.2rem; font-weight: 700; color: var(--color-primary); background: #fff; }
  :global(.cmp-box.revealed) { border-color: var(--color-primary); background: rgba(37, 99, 235, 0.06); }
  @media (max-width: 640px) {
    :global(.msr-problem) { flex-direction: column !important; align-items: flex-start !important; }
  }
  @media print {
    :global(.cmp-box) { border-color: #333; }
  }
</style>
